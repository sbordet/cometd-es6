{
  "name": "cometd",
  "version": "0.0.1",
  "description": "A CommonJS port of the CometD client.",
  "main": "index.js",
  "scripts": {
    "prepublish": "browserify index.js --standalone CometD --outfile dist/cometd.js -t [ babelify --presets [ es2015 ] ]"
  },
  "author": "",
  "license": "Apache 2.0",
  "devDependencies": {
    "babel-preset-es2015": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0"
  }
}
