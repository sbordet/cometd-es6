{
  "name": "cometd",
  "version": "0.0.1",
  "description": "A CommonJS port of the CometD client.",
  "main": "index.js",
  "scripts": {
    "prepublish": "mkdir dist && browserify index.js --standalone CometD --outfile dist/cometd.js"
  },
  "author": "",
  "license": "Apache 2.0",
  "devDependencies": {
    "browserify": "^13.0.0"
  }
}
